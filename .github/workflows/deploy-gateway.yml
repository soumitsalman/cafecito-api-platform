name: Gateway CI

on:
  push:
    branches: [main]
    paths-ignore:
      - "services/**"
  pull_request:
    paths-ignore:
      - "services/**"
  workflow_dispatch:

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm

      - run: npm ci
      - run: npm run lint
