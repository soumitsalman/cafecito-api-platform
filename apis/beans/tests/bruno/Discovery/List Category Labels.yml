info:
  name: List Category Labels
  type: http
  seq: 1
  tags:
    - Discovery
http:
  method: GET
  url: "{{baseUrl}}/categories"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "data": [
              {
                "type": "",
                "value": ""
              }
            ],
            "meta": {
              "as_of": ""
            },
            "pagination": {
              "limit": 0,
              "next_cursor": "",
              "num_results": 0
            }
          }
  - name: 400 Response
    description: Bad Request
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 400
      statusText: Bad Request
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
  - name: 401 Response
    description: Unauthorized
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 401
      statusText: Unauthorized
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
  - name: 500 Response
    description: Internal Server Error
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 500
      statusText: Internal Server Error
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
  - name: 429 Response
    description: Too Many Requests
    request:
      url: "{{baseUrl}}/categories"
      method: GET
    response:
      status: 429
      statusText: Too Many Requests
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": ""
          }
docs: >-
  Answers: Which normalized category labels can I use as Article filters?

  Returns: A cursor-paginated collection of category labels used by Article
  records.

  Use when: an agent has an unknown category spelling and needs discovery before
  Article filtering.

  Do not use when: a known normalized label is already available; query Articles
  directly.

  Search/filter behavior: q performs case-insensitive discovery matching;
  returned values are normalized labels.

  Time: no time filter applies.

  Sort/pagination: follow pagination.next_cursor unchanged as cursor. limit
  defaults to 20 and is capped at 100.

  Missing fields: the collection may be empty; this route does not define a
  taxonomy hierarchy.

  Next step: carry a returned value into the categories Article filter.
